-- Day 2 --
git add remote "url" - to link up the git repository
git pull origin master - pull git

git remote rename origin upstream
create a fork from the notes
git remote

css:
can set by class or give an element id
id only can use once
class can use many times
.class {type css}
#id {type css}

-- Day 3 --
Making a text appear when you hover over a button
button {}
button:hover + .container {
display: block;
}
.container {
display: none:
}

— Day 4 —
1) create all the content
2) create all the selectors in css
3) slowly input the css
4) javascript - make it interactive

always use the === to avoid complications

arguments are what happen in functions, parameters are what you put inside
function (x) - x is the argument
function (123) - 123 is the parameter
