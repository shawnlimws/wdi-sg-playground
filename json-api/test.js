var expect = require('chai').expect

describe('server', () => {
  it('should respond with json data', () => {
    expect
  })
  it('should provide a list of all class participants', () => {

  })
  it('should allow searching by name', () => {

  })
  it('should allow sorting alphabteically by name', () => {

  })
  it('should allow whitelisting of specific fields', () => {

  })
})
